else if (type.equals("DECIMAL")) {
    try {
        BigDecimal decimalValue = new BigDecimal(value);
        pstmt.setBigDecimal(index, decimalValue);
    } catch (NumberFormatException e) {
        throw new SQLException(
                "Error parsing DECIMAL value: " + value, e
        );
    }
}
